/**
 * Module definition file.
 */
define(['angular'], function(angular) {
    'use strict';

    var moduleName = "<%= moduleNameString1 %>";
    angular.module(moduleName, []);

    console.debug('Module created: [' + moduleName + ']');
    return moduleName;
});
//RouteInsertReference
